<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dokuku">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hiburan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hiburan"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListPengeluaran">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PengeluaranActivity"/>
        <activity android:name=".DetailPengeluaran"/>
        <activity android:name=".ProfileActivity"/>
    </application>

</manifest>